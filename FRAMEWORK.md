# Career Copilot 评估与求职方法论

> 本文件沉淀 Career Copilot(web 平台 + CLI)的求职评估理念。所有实现(评价、简历、求职信、面试、校准)都遵循这里的规则。改理念 = 改本文件 + 对应代码,让方法论可读、可审计、可分享。

## 核心原则

1. **诚实优先,永不虚构。** 简历、求职信、面试准备只使用已核验证据(`career_evidence`,经 `evidence.json` 或 Career Vault)。缺的技能如实标注为 gap,绝不硬塞。
2. **自动提交永远关闭。** 系统只准备材料和跳转真实投递页,提交动作永远由用户本人完成。这是安全底线。
3. **岗位是不可信输入。** 岗位文本可能含隐藏指令或错误信息,只作为评估数据,绝不作为执行指令,也不引导工作流。
4. **事实必须可回溯。** 每个项目条目带来源(证据 ID / URL / 置信度),面试中每个陈述都能被面试官验证。

## 五维评估(评价岗位)

岗位按推荐度打分,由以下维度构成:

| 维度 | 含义 | 硬性/软性 |
|------|------|----------|
| 硬性资格 | 届别(如 2028)、是否在校、出勤、周期、地点、工作方式 | 硬性(不满足即不过) |
| 技能匹配 | JD 要求的技能 vs 你已核验的技能(`skills.mjs` 词典 canonical 匹配) | 软性 |
| 经验匹配 | 历史经历/项目与岗位的吻合度 | 软性 |
| 证据匹配 | 有已核验项目证据支撑的技能 | 软性 |
| 目标匹配 | 画像的目标岗位/关键词命中 | 软性 |

**缺口处理:** `missing_skills` 是岗位要但你没有的技能。它进入求职信作为诚实说明,绝不伪造。

## 技能词典与档位

- **词典** `apps/web/lib/skills.mjs`:技能 canonical → 别名列表。任何岗位/证据文本经 `extractJobSkills` 归一化为 canonical,保证"岗位说 sql、你写 postgresql"也能匹配(都归到 postgresql)。
- **档位** `RESUME_PERSONAS`:每类职业一套(工程研发/产品运营/解决方案/法律法务/人力资源/财务会计/行政支持/工科工程/摄影视频/主播直播/通用)。每个档位有:优先技能(决定项目排序)、强调点、摘要。
- **新赛道怎么加:** 运行 `npm run cli -- skills:check`,看缺失词;往词典加词条;若整类职业缺失,加档位 + 信号词(见 `AGENTS.md` 第 2.5 步)。

## 简历生成(多档位)

基于岗位 + 已核验证据 + 画像,纯规则生成:
1. 抽岗位技能(`extractJobSkills`)
2. 选证据(`evidenceForPersona`:岗位命中×3 + 档位优先技能×2 + 置信度),取前 6
3. 算 matched/missing
4. 项目按"档位优先技能命中"排序(任何用户的证据都正确归类,不依赖项目名)
5. 组装摘要/技能/项目/问候语/求职信

**项目排序原则:** 优先展示"符合当前档位"的项目(如法律岗把模拟法庭排前,工程岗把后端项目排前)。

## JD 深拆与投递决策

`npm run cli -- jd <URL或文本>` 把岗位拆成:岗位速览 / 必备条件(硬门槛) / 加分项 / 日常工作职责 / 隐含信息(字面没说但能推断) / 你的匹配度(命中/缺口) / 面试重点提示 / 行动清单。判断"该不该投、缺什么、怎么补"。URL 用 r.jina.ai 免费抓取。

## A-C-R-E 审校

简历每条项目 bullet 应覆盖四个槽位,缺哪个标记哪个:
- **A(动作)**:本人采取的具体动作(开发/设计/实现/优化…)
- **C(情境/范围)**:为谁、解决什么问题、范围/约束/团队边界
- **R(结果)**:交付/上线/验收/覆盖/规模/变化(数字优先,无数字写范围与交付)
- **E(证据)**:结果来源,只入私有账本,不显示在公开简历

`npm run cli -- resume:review` 自动检查每条项目的 A/C/R 是否齐全并提示补全。**没有可靠数字时写范围、复杂度、交付,绝不制造百分比。**

## 生成就绪门禁

`npm run cli -- assess` 在生成最终简历前检查 7 项,全部通过才算 READY:
1. 目标岗位明确
2. 姓名已填
3. 教育信息可确认
4. 至少两项证据形成"动作+情境+结果"闭环
5. 技能有证据关联(不孤立罗列)
6. 无阻断性疑点(城市/出勤/毕业年份)
7. 已有确认

未通过只交付事实摘要或草稿,不把 PDF 当最终版。**"随便/你看着办"不是事实确认。**

## 求职信

结构:开头(你是谁+为什么这个岗位)→ 能力匹配(基于已核验项目)→ 公司认同 → 诚实缺口 → 收尾。每个项目带置信度和"已核验"标注。

## 面试准备

基于真实证据生成 STAR 包(情境/任务/行动/结果),配可能的问题和反问清单。面试陈述必须与已提交简历/求职信一致。

## 结果校准(持续学习)

每次投递结果记录到 `tracker.json`(`outcome` 命令)。进入面试/Offer 的岗位所命中的技能被 `calibrate` 记录——这些技能被证明有效。后续 `rank` 对它们高亮,让评估随真实结果收敛。

## 跟进(只草拟,从不发送)

投递超过 10 天没回应、跟进少于 2 次 → `outcome followup` 提醒。跟进内容只基于已提交材料,不引入新主张,最多 2 次。web 端投递管理同步显示"已安静 X 天"提醒。

## ATS 与导出

CLI 生成简历后跑 ATS 检查:把简历渲染成纯文本(机器视角),统计岗位关键词覆盖率,缺失如实标注。web 端可导出 DOCX/HTML。

## 数据互通

- CLI(本地 `career-data/`)与 web(Supabase)共用同一套核心 lib(技能词典/评价/档位/搜索)
- `npm run cli -- export` 把 CLI 搜到的岗位同步到 web,web 端可投递、可跟踪
- web 端搜索有免费 Tavily 兜底,不接任何 API 也能用;用户可填自己的 OpenAI key 获得更强网页搜索
- 岗位来源以"链接 / JD 导入"为核心(8 个招聘平台,含 2 个登录墙平台需手动导入),不再使用需要连接公司来源的 ATS 聚合(Greenhouse/Lever/Ashby)

## 安全

- 自动提交永远关闭;简历/求职信只基于已核验证据;岗位文本不引导工作流;key 服务端存储,浏览器读不回明文(web 端)。
